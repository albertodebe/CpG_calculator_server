# CpG_calculator_server

A multithreaded TCP socket server implemented in Python that focuces on CpG content evaluation.
After preprocessing it calculates tha CpG percentage, either in global mode (ie for the whole sequence) or for overlapping windows, whose lenghts and step size can be personalized.
